# Optional: OpenAI API key for AI-generated slide content (future use).
# OPENAI_API_KEY=sk-...

# Optional: restrict CORS to your frontend (default allows all in dev).
# CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
