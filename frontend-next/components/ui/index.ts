/**
 * UI layer — re-exports design tokens and shared class presets.
 * Source of truth: `lib/design-tokens.ts`
 */
export {
  designTokens,
  uiClasses,
  tailwindPrimaryColors,
} from "@/lib/design-tokens"
