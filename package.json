{
  "name": "slidemaker",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "build": "cd frontend-next && npm ci && npm run build",
    "dev": "npm run dev --prefix frontend-next",
    "start": "npm run start --prefix frontend-next",
    "dev:frontend": "npm run dev --prefix frontend-next",
    "dev:backend": "node scripts/run-backend.mjs",
    "dev:all": "node scripts/dev-all.mjs",
    "dev:all:windows": "powershell -NoProfile -ExecutionPolicy Bypass -File ./dev-all.ps1"
  }
}
